Amann, a pure storie.

```

# To develop
npm develop

# To build
npm build

# To test SSR (for Lighthouse etc.)
npm ssr

# To format JS (precommit)
npm format

# To generate favicons (included in `build`)
npm build:favicons
```
